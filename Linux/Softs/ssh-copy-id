if [[ $# != 1 ]]
then 
	echo "USAGE: ssh-copy-id '-p port user@host'"
else
	cat ~/.ssh/id_*.pub | ssh $1 "umask 077; test -d ~/.ssh || mkdir ~/.ssh ; cat >> ~/.ssh/authorized_keys" || echo "failed" 
fi
