export MOZ_PLUGIN_PATH=""
if [ -e /home/guillaume/.proxy_is_set ]
then
    no_proxy=localhost,127.0.0.1,*.example.com http_proxy=http://cache.polytechnique.fr:8080 https_proxy=http://cache.polytechnique.fr:8080 midori $*
else
    midori $*
fi
