export WORKSPACE_DIR=/home/guillaume/Work/Code

export CLASSPATH=${WORKSPACE_DIR}/presans-commons/classes
export CLASSPATH=${CLASSPATH}:${WORKSPACE_DIR}/presans-crawling/classes
export CLASSPATH=${CLASSPATH}:${WORKSPACE_DIR}/presans-indexing/classes
export CLASSPATH=${CLASSPATH}:${WORKSPACE_DIR}/presans-experts/classes
export CLASSPATH=${CLASSPATH}:${WORKSPACE_DIR}/presans-searching/classes

for i in ${WORKSPACE_DIR}/presans-lib/jars/*.jar
do
        export CLASSPATH=${CLASSPATH}:${i}
done
